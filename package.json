{
  "name": "onegraphwebapp",
  "version": "0.1.0",
  "main": "server.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build-run": "cd client && yarn build && cd .. && node server.js",
    "heroku-postbuild": "cd client && yarn install && yarn build"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongodb": "^3.4.1",
    "mongoose": "^5.8.3"
  },
  "author": "Strontium",
  "license": "MIT"
}
